

<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>

  <% end %>
</ul>
<%= will_paginate %>
<%= link_to image_tag("secret.jpg", alt: "Secrets logo"),
            'http://rubyonrails.org/' %>
            <br>
  <%= link_to "Sign up now!", 'users/new', class: "btn btn-lg btn-primary" %>
